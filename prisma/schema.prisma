generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id      BigInt @id @default(autoincrement())
  username    String @unique
  numFish Int?   @map("num_fish")
  passwordHash String @map("password_hash")
  
  @@map("users")
}
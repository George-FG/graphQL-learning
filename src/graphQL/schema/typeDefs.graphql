type Query {
  getUserByID(ID: ID!): User!
}

type Mutation {
  signUp(username: String!, password: String!, numFish: Int!): AuthUser!
  login(username: String!, password: String!): AuthUser!
}

type User {
  ID: ID!
  Username: String!
  NumFish: Int
}

type AuthUser {
  User: User!
}
